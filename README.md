# Client-Analysis-For-Banking
SQL query generating a Banking Customer Analytical Record (CAR) for Machine Learning. Performs feature engineering (RFM, one-hot encoding, age) by aggregating transactional data. Handles NULLs and prevents survival bias to create a clean, ML-ready dataset.
